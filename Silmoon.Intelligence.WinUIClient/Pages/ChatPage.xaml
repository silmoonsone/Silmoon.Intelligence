<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Silmoon.Intelligence.WinUIClient.Pages.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Silmoon.Intelligence.WinUIClient.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:pages="using:Silmoon.Intelligence.WinUIClient.Pages"
    xmlns:models="using:Silmoon.Intelligence.WinUIClient.Models"
    xmlns:ctk="using:CommunityToolkit.WinUI.UI.Controls"
    d:DataContext="{d:DesignInstance Type=local:ChatPageViewModel}"
    NavigationCacheMode="Required"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <!--左侧聊天列表布局-->
        <Grid Grid.Column="0">
            <Grid BorderBrush="Gray" BorderThickness="1" CornerRadius="10" Margin="0,0,10,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0" Margin="5">
                </StackPanel>
                <ListView x:Name="nameChatList" x:FieldModifier="public" Grid.Row="1" ItemsSource="{x:Bind ViewModel.ChatList, Mode=TwoWay}" SelectedIndex="{x:Bind ViewModel.SelectedChatListIndex}" SelectedItem="{x:Bind ViewModel.SelectedChatListItem, Mode=TwoWay}" Padding="3">
                    <ListView.ContextFlyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="新会话" Command="{x:Bind ViewModel.NewChatCommand}" />
                        </MenuFlyout>
                    </ListView.ContextFlyout>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ChatListItem">
                            <StackPanel Margin="5">
                                <StackPanel.ContextFlyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem Text="复制" Command="{x:Bind This.CopyChatCommand}" CommandParameter="{x:Bind}" />
                                        <MenuFlyoutItem Text="删除" Command="{x:Bind This.DeleteChatCommand}" CommandParameter="{x:Bind}" />
                                        <MenuFlyoutItem Text="生成标题" Command="{x:Bind This.GenerateTopicCommand}" CommandParameter="{x:Bind}" />
                                        <!--<MenuFlyoutSeparator />
                                        <MenuFlyoutItem Text="属性" />-->
                                    </MenuFlyout>
                                </StackPanel.ContextFlyout>
                                <TextBlock Text="{x:Bind Topic, Mode=OneWay}" TextTrimming="CharacterEllipsis"></TextBlock>
                                <TextBlock Foreground="Gray">
                                    <Run Text="{x:Bind ChatCounting, Mode=OneWay, Converter={StaticResource StringFormatConverter}, ConverterParameter='({0})'}" FontSize="10"></Run>
                                    <Run Text="{x:Bind LastAt, Mode=OneWay}" FontSize="10"></Run>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Grid>
        <!--右侧聊天详情布局-->
        <Grid Grid.Column="1" RowSpacing="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="auto"></RowDefinition>
            </Grid.RowDefinitions>
            <!--最顶端布局-->
            <Frame Grid.Row="0" Padding="5">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{x:Bind ViewModel.ModelName, Mode=OneWay}" HorizontalAlignment="Center" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"></TextBlock>
                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="5">
                        <Button Command="{x:Bind ViewModel.UndoHistoryCommand}">
                            <FontIcon Glyph="&#xE7A7;" />
                        </Button>
                        <Button Command="{x:Bind ViewModel.ClearHistoryCommand}">
                            <FontIcon Glyph="&#xE74D;" />
                        </Button>
                        <Button Command="{x:Bind ViewModel.SaveHistoryCommand}">
                            <FontIcon Glyph="&#xE74E;" />
                        </Button>
                        <Button Command="{x:Bind ViewModel.NewChatCommand}">
                            <FontIcon Glyph="&#xE710;" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Frame>
            <!--中间内容布局-->
            <Grid Grid.Row="1">
                <Frame BorderThickness="1" BorderBrush="Gray" Padding="0,10,0,10" CornerRadius="10">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"></RowDefinition>
                            <RowDefinition Height="auto"></RowDefinition>
                        </Grid.RowDefinitions>
                        <ListView Grid.Row="0" x:Name="nameChat" ItemsSource="{x:Bind ViewModel.Items, Mode=TwoWay}" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:ChatItem">
                                    <StackPanel Margin="5" BorderThickness="1" CornerRadius="10" Padding="5" BorderBrush="Gray">
                                        <TextBlock Text="{x:Bind Role, Converter={StaticResource EnumDisplayConverter}, Mode=OneWay}" Margin="0,0,0,5" FontSize="18" FontWeight="Bold"/>

                                        <TextBlock Text="{x:Bind StreamingReasoningContent, Mode=OneWay}" IsTextSelectionEnabled="True" TextWrapping="Wrap" Visibility="{x:Bind StreamingReasoningContentVisual, Converter={StaticResource VisibilityToBoolConverter}, Mode=OneWay}" Foreground="Gray" FontSize="12" Margin="0,0,0,5" />
                                        <TextBlock Text="{x:Bind StreamingContent, Mode=OneWay}" IsTextSelectionEnabled="True" TextWrapping="Wrap" Visibility="{x:Bind StreamingContentVisual, Converter={StaticResource VisibilityToBoolConverter}, Mode=OneWay}" />

                                        <TextBlock Text="{x:Bind ReasoningContent, Mode=OneWay}" IsTextSelectionEnabled="True" TextWrapping="Wrap" Visibility="{x:Bind ReasoningContentVisual, Converter={StaticResource VisibilityToBoolConverter}, Mode=OneWay}" Foreground="Gray" FontSize="12" Margin="0,0,0,5" />
                                        <ctk:MarkdownTextBlock Text="{x:Bind Content, Mode=OneWay}" IsTextSelectionEnabled="True" Background="Transparent" TextWrapping="Wrap" Visibility="{x:Bind ContentVisual, Converter={StaticResource VisibilityToBoolConverter}, Mode=OneWay}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                        <TextBlock Margin="25,5,0,0" Grid.Row="1" Text="{x:Bind ViewModel.UsageInfo, Mode=OneWay}" FontSize="12" Foreground="Gray" Visibility="{x:Bind ViewModel.UsageInfo, Converter={StaticResource HasContentToVisibilityConverter}, Mode=OneWay}"></TextBlock>
                    </Grid>
                </Frame>
            </Grid>
            <!--工具执行状况布局-->
            <Frame Grid.Row="2">
                <StackPanel>
                    <ListView ItemsSource="{x:Bind ViewModel.ToolExecuteIndicators, Mode=OneWay}" SelectionMode="None" ScrollViewer.HorizontalScrollMode="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsStackPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="models:ToolExecuteIndicatorModel">
                                <StackPanel BorderThickness="1" Margin="-15,0,0,10" CornerRadius="10" Padding="5" BorderBrush="Gray">
                                    <TextBlock Text="{x:Bind FunctionName, Mode=OneWay}" FontWeight="ExtraBlack" FontSize="12"/>
                                    <TextBlock Text="{x:Bind Status, Mode=OneWay}" FontSize="10" Foreground="{x:Bind Color, Mode=OneWay}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </StackPanel>
            </Frame>
            <!--聊天输入框布局-->
            <StackPanel Grid.Row="3">
                <Grid ColumnSpacing="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"></ColumnDefinition>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                        <ColumnDefinition Width="auto"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBox
                    x:Name="nameUserInput"
                    Grid.Column="0"
                    PlaceholderText="和智能体对话（Enter 发送，Shift+Enter 换行）"
                    Text="{x:Bind ViewModel.UserInput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                    TextWrapping="Wrap"
                    MaxHeight="200"
                    ScrollViewer.VerticalScrollBarVisibility="Auto"
                    KeyDown="nameUserInput_KeyDown" />
                    <Button Grid.Column="1" Command="{x:Bind ViewModel.SelectImagesCommand}">
                        <FontIcon Glyph="&#xE8B9;"  />
                    </Button>
                    <Button Grid.Column="2" Content="发送" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.SendChatCommand}" IsEnabled="{x:Bind ViewModel.UserInputAvaliable, Mode=OneWay}"></Button>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
